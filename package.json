{
  "name": "point-system-monorepo",
  "version": "1.0.0",
  "description": "Point System Monorepo with NestJS backend and Next.js frontends",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/ui",
    "apps/backend",
    "apps/admin",
    "apps/user"
  ],
  "scripts": {
    "start": "cd apps/backend && npm start",
    "start:backend": "cd apps/backend && npm start",
    "start:admin": "cd apps/admin && npm start",
    "start:user": "cd apps/user && npm start",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:admin\" \"npm run dev:user\"",
    "dev:backend": "cd apps/backend && npm run dev",
    "dev:admin": "cd apps/admin && npm run dev",
    "dev:user": "cd apps/user && npm run dev",
    "build": "npm run build:shared && npm run build:ui && npm run build:admin && npm run build:user",
    "build:shared": "cd packages/shared && npm run build",
    "build:ui": "cd packages/ui && npm run build",
    "build:backend": "cd apps/backend && npm run build",
    "build:admin": "cd apps/admin && npm run build",
    "build:user": "cd apps/user && npm run build",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces && rimraf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
